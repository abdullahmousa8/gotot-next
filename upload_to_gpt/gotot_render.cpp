#include "gotot_render.h"

#include <iostream>

void GototRender::initialize() {
    std::cout << "[GOTOT-NEXT] GototRender initialized." << std::endl;
}

void GototRender::shutdown() {
    std::cout << "[GOTOT-NEXT] GototRender shutdown." << std::endl;
}